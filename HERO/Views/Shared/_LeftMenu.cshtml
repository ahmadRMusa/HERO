<!-- left, vertical navbar -->
<div class="col-md-2 bootstrap-admin-col-left">
    <ul class="nav navbar-collapse collapse bootstrap-admin-navbar-side">
        @Html.MenuItem("About", "Index", "Home")
        @if (User.IsInRole("Admin"))
        {
            @Html.MenuItem("Weekly Classes", "Index", "WeeklyClassSetups")
            @Html.MenuItem("Single Classes", "Index", "SingleClassSetups")
            @Html.MenuItem("Calendar", "Index", "Classes")
        }
        else
        {
            @Html.MenuItem("Calendar", "Index", "Classes")
            @Html.MenuItem("Upcoming Classes", "UpcomingClasses", "Classes")
            @Html.MenuItem("Past Classes", "PastClasses", "Classes")
        }
    </ul>
</div>

