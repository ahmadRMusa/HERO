@model IEnumerable<HERO.Models.Objects.SingleClass>

@{
    ViewBag.Title = "Manage Individual Classes";
}

@Html.Partial("_LeftMenu")

<div class="col-md-10">
    <div class="row">
        <div class="bootstrap-admin-content-title">
            <h1>Manage Individual Classes</h1>
        </div>
    </div>
    <div class="row">
        <div class="panel panel-default">
            <div class="panel-heading">
                <div class="text-muted bootstrap-admin-box-title">Current Individual Classes</div>
            </div>
            <div class="bootstrap-admin-panel-content">
                <table class="table">
                    <thead>
                        <tr>
                            <th>@Html.DisplayNameFor(m => m.Type)</th>
                            <th>@Html.DisplayNameFor(m => m.Date)</th>
                            <th>@Html.DisplayNameFor(m => m.Duration)</th>
                            <th>@Html.DisplayNameFor(m => m.MaxAttendance)</th>
                            <th>Actions</th>
                        </tr>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>@Html.DisplayFor(m => item.Type)</td>
                                <td>@Html.DisplayFor(m => item.Date)</td>
                                <td>@Html.DisplayFor(m => item.Duration)</td>
                                <td>@Html.DisplayFor(m => item.MaxAttendance)</td>
                                <td>
                                    @Html.ActionLink("Details", "Details", new { id = item.Id }, new { @class = "btn btn-xs btn-default" })
                                </td>
                            </tr>
                        }
                    </thead>
                </table>
            </div>
        </div>
    </div>
</div>
@Html.ActionLink("Add an Individual Class", "Create", "SingleClasses", new { @class = "btn btn-lg btn-primary" })